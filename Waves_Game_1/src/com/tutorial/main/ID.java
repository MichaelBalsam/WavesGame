package com.tutorial.main;

public enum ID {

	player(),                       // allows us to id something as a player 
	BasicEnemy(),                   // allows us to id something as a enemy
	FastEnemy(),                    // allows us to id something as a fast enemy
	SmartEnemy(),                   // allows us to id something as a Smart tracking enemy
	Boss1(),                        // allows us to id something as a boss enemy
	Trail();                        // allows us to id an enemies trail 
}
